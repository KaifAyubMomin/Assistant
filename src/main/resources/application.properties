spring.application.name=Users
server.port=8083

# Spring profiles configuration
# Default active profile (can be overridden with command line or environment)
spring.profiles.active=local

# Common settings (can be overridden by profile-specific files)
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
